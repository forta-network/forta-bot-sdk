{
  "name": "forta-bot-starter",
  "version": "0.0.1",
  "description": "Forta Bot Javascript starter project",
  "chainIds": [
    1
  ],
  "scripts": {
    "start": "npm run start:dev",
    "start:dev": "nodemon --watch src --watch forta.config.json -e js,json --exec \"forta-bot run\"",
    "start:prod": "forta-bot run --prod",
    "tx": "forta-bot run --tx",
    "block": "forta-bot run --block",
    "range": "forta-bot run --range",
    "file": "forta-bot run --file",
    "publish": "forta-bot publish",
    "info": "forta-bot info",
    "logs": "forta-bot logs",
    "push": "forta-bot push",
    "disable": "forta-bot disable",
    "enable": "forta-bot enable",
    "keyfile": "forta-bot keyfile",
    "test": "jest"
  },
  "dependencies": {
    "forta-agent": "^0.1.9"
  },
  "devDependencies": {
    "jest": "^27.0.6",
    "nodemon": "^2.0.8"
  }
}
